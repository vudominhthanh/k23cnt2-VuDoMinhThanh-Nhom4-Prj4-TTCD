// Ví dụ xử lý nút cộng trừ trong giỏ hàng (thêm vào đoạn script tối ưu)
functionDiv.addEventListener('click', (e) => {
    if (e.target.innerText === '+') {
        // Logic tăng số lượng
    }
    if (e.target.innerText === '-') {
        // Logic giảm số lượng
    }
});